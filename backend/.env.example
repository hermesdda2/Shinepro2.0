PORT=4000
NODE_ENV=production

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_NAME=shinepro

ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your_secure_password
JWT_SECRET=your_random_jwt_secret_min_32_chars
JWT_EXPIRES_IN=24h

# FRONTEND_URL only needed in dev (when frontend runs on a different port)
# FRONTEND_URL=http://localhost:5173
